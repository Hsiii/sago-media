{
  "name": "sago-media-workspace",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "web"
  ],
  "scripts": {
    "dev": "PR_MEDIA_ROOT=.data/media MEDIA_ADMIN_TOKEN=local-development bun run --parallel dev:server dev:web",
    "dev:server": "bun --watch server/index.ts",
    "dev:web": "bun run --cwd web dev",
    "build": "bun run build:web && bun run build:server",
    "build:server": "bun build server/index.ts --target=bun --outfile=dist/server.js",
    "build:web": "bun run --cwd web build",
    "check": "bun run test && bun run check:web && bun run build:server",
    "check:web": "bun run --cwd web check",
    "test": "bash tests/api && bash tests/pipeline"
  }
}
